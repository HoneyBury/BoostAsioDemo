Gateway Server has been deployed.

Check deployment status:
  kubectl get deployment -n {{ .Release.Namespace }} {{ include "gateway-server.fullname" . }}
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Check service:
  kubectl get service -n {{ .Release.Namespace }} {{ include "gateway-server.fullname" . }}

Check HPA:
  kubectl get hpa -n {{ .Release.Namespace }} {{ include "gateway-server.fullname" . }}-hpa

Check PDB:
  kubectl get pdb -n {{ .Release.Namespace }} {{ include "gateway-server.fullname" . }}-pdb

Upgrade:
  helm upgrade {{ .Release.Name }} k8s/helm/gateway-server/ -n {{ .Release.Namespace }}

Uninstall:
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}
