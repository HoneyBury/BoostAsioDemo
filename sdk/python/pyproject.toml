[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "boost-gateway-sdk"
version = "3.4.0"
description = "Boost Gateway Game Server SDK"
requires-python = ">=3.8"
